# 搭梯子 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a simplified "家长讲题急救" tool that outputs a structured 5-module teaching plan when a parent inputs a math problem.

**Architecture:** Add a `/v2` route to the existing FastAPI app that serves a new single-page HTML frontend. A new `/api/v2/generate` endpoint calls DeepSeek with a simplified prompt and returns structured JSON (not streaming). The existing v1 routes remain untouched.

**Tech Stack:** FastAPI (existing), DeepSeek API (existing), vanilla HTML/CSS/JS single-page frontend, python-dotenv (existing)

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `main.py` | Modify | Add `/v2` route + `/api/v2/generate` endpoint |
| `templates/index2.html` | Create | Single-page 2.0 UI (input + result views) |
| `static/style2.css` | Create | 2.0 styles: warm white/orange, mobile-first |
| `static/app2.js` | Create | Form submission, loading state, result render, copy |

---

## Task 1: Add v2 API endpoint to main.py

**Files:**
- Modify: `main.py`

- [ ] **Step 1: Read current end of main.py to find insertion point**

Open `main.py` and locate the last route definition (around the `@app.get("/jinkuang")` block).

- [ ] **Step 2: Add GenerateV2Request model and `/api/v2/generate` endpoint**

Add the following to the bottom of `main.py`, before or after the jinkuang route:

```python
# ── 搭梯子 2.0 ──────────────────────────────────────────────

MOCK_DATA = {
    "success": True,
    "data": {
        "start_from": "先和孩子确认这道题最后要求什么，再引导他理解"6天完成"表示一天只完成一部分。",
        "stuck_points": [
            "孩子最容易卡在：不理解"6天完成"为什么能转成"一天完成多少"。",
            "也容易把"合作"误以为是把6和8直接相加。",
        ],
        "fallback_step": "如果孩子没懂"效率"，先退回去讲：整项工程看成一整块，一天只做其中的一部分。",
        "direct_script": "我们先不急着算答案。先看这题最后要求什么。想知道几天完成，就要先知道他们一天一共做多少，对不对？",
        "three_steps": {
            "say_it": "先让孩子说清：这题最后求什么，为什么第一步要先求一天完成多少。",
            "write_it": "让孩子写出甲一天完成1/6，乙一天完成1/8，再写合作一天完成多少。",
            "draw_it": "如果还是不懂，就把整项工程画成一条，再标出一天做的部分。",
        },
    },
}

V2_SYSTEM_PROMPT = """你是一个"小学生家长讲题急救助手"。

你的任务不是生成长篇解析，也不是只给答案，而是在家长输入一道小学数学题后，帮助家长立刻知道"这道题现在该怎么讲"。

请始终围绕以下目标输出：
1. 告诉家长这题先从哪里讲；
2. 告诉家长孩子最容易卡在哪；
3. 告诉家长如果孩子没懂，应该退回哪一步；
4. 给家长一小段可以直接照着说的话；
5. 用"先说出来、再写出来、必要时画出来"的方式，帮助家长判断孩子有没有真正听懂。

请严格遵守：
- 输出对象首先是家长；
- 必须根据孩子当前年级决定讲法，不能调用高于该年级的背景知识；
- 不要长篇理论，不要像老师教案；
- 要短、准、能立刻用；
- 不要只给答案，要给"讲法"和"退路"；
- 优先解决"现在怎么办"，而不是追求面面俱到。

请严格输出 JSON，不要输出额外解释，不要输出 markdown 代码块标记。
JSON 字段必须包含：
start_from,
stuck_points（数组，1-2条）,
fallback_step,
direct_script,
three_steps: { say_it, write_it, draw_it }"""


def build_v2_user_prompt(grade: str, question: str, student_answer: str, parent_note: str) -> str:
    return f"""请根据下面信息，生成这道题的"家长讲题方案"。

【孩子年级】
{grade}

【题目】
{question}

【孩子原答案（可选）】
{student_answer or "未填写"}

【家长补充说明（可选）】
{parent_note or "未填写"}

要求：
- 输出要适合小学生家长阅读；
- 不要写成长篇报告；
- 要让家长有"我现在知道怎么讲了"的感觉；
- 如果题目本身信息不足，请尽量指出并做最合理理解。"""


class GenerateV2Request(BaseModel):
    grade: str
    question: str
    student_answer: str = ""
    parent_note: str = ""


@app.get("/v2", response_class=HTMLResponse)
async def index_v2(request: Request):
    return templates.TemplateResponse("index2.html", {"request": request})


@app.post("/api/v2/generate")
async def generate_v2(req: GenerateV2Request):
    # Mock mode: if no API key configured, return mock data for UI testing
    if not DEEPSEEK_API_KEY:
        return MOCK_DATA

    try:
        async with httpx.AsyncClient(timeout=60.0) as client:
            resp = await client.post(
                "https://api.deepseek.com/chat/completions",
                headers={
                    "Authorization": f"Bearer {DEEPSEEK_API_KEY}",
                    "Content-Type": "application/json",
                },
                json={
                    "model": "deepseek-chat",
                    "messages": [
                        {"role": "system", "content": V2_SYSTEM_PROMPT},
                        {
                            "role": "user",
                            "content": build_v2_user_prompt(
                                req.grade, req.question, req.student_answer, req.parent_note
                            ),
                        },
                    ],
                    "temperature": 0.3,
                    "max_tokens": 1200,
                    "response_format": {"type": "json_object"},
                },
            )
        data = resp.json()
        raw_content = data["choices"][0]["message"]["content"]

        # Parse JSON from model response
        result = extract_json_object(raw_content)

        # Validate required fields; fill defaults if missing
        result.setdefault("start_from", "")
        result.setdefault("stuck_points", [])
        result.setdefault("fallback_step", "")
        result.setdefault("direct_script", "")
        result.setdefault("three_steps", {"say_it": "", "write_it": "", "draw_it": ""})

        return {"success": True, "data": result}

    except Exception as e:
        return {"success": False, "error": f"生成失败，请稍后再试。({str(e)})"}
```

- [ ] **Step 3: Verify main.py runs without syntax errors**

```bash
cd /Users/yangming/datizi && python -c "import main" && echo "OK"
```
Expected: `OK` with no errors.

- [ ] **Step 4: Commit**

```bash
cd /Users/yangming/datizi
git add main.py
git commit -m "feat: add v2 API endpoint and mock mode for 搭梯子 2.0"
```

---

## Task 2: Create static/style2.css

**Files:**
- Create: `static/style2.css`

- [ ] **Step 1: Create style2.css**

```css
/* 搭梯子 2.0 — style2.css */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --orange-500: #f97316;
  --orange-600: #ea580c;
  --orange-50:  #fff7ed;
  --orange-100: #ffedd5;
  --gray-50:  #f9fafb;
  --gray-100: #f3f4f6;
  --gray-200: #e5e7eb;
  --gray-400: #9ca3af;
  --gray-500: #6b7280;
  --gray-700: #374151;
  --gray-900: #111827;
  --radius: 14px;
  --shadow: 0 1px 3px rgba(0,0,0,.08), 0 4px 12px rgba(0,0,0,.06);
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
               "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  background: var(--gray-50);
  color: var(--gray-900);
  line-height: 1.65;
  min-height: 100vh;
}

/* ── Header ── */
.site-header {
  background: linear-gradient(135deg, #92400e 0%, #c2410c 50%, #f97316 100%);
  padding: 36px 20px 32px;
  text-align: center;
  color: #fff;
}
.header-icon { font-size: 2.2rem; margin-bottom: 8px; }
.site-header h1 { font-size: 2rem; font-weight: 800; letter-spacing: .06em; margin-bottom: 6px; }
.site-header .tagline { font-size: 1rem; opacity: .9; margin-bottom: 4px; }
.site-header .sub-tagline { font-size: .82rem; opacity: .72; }

/* ── Layout ── */
.container { max-width: 680px; margin: 0 auto; padding: 28px 16px 60px; }

/* ── Card ── */
.card {
  background: #fff;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 28px;
  margin-bottom: 20px;
}
@media (max-width: 500px) { .card { padding: 20px 16px; } }

/* ── Intro text ── */
.intro-text { font-size: .95rem; color: var(--gray-700); line-height: 1.7; margin-bottom: 24px; }

/* ── Form ── */
.form-group { margin-bottom: 20px; }
.form-label {
  display: block; font-size: .875rem; font-weight: 600;
  color: var(--gray-700); margin-bottom: 8px;
}
.required { color: var(--orange-500); margin-left: 2px; }

/* Grade selector */
.grade-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 8px; }
@media (max-width: 500px) { .grade-grid { grid-template-columns: repeat(3, 1fr); } }

.grade-btn {
  padding: 9px 4px; border: 1.5px solid var(--gray-200);
  border-radius: 8px; background: #fff; font-size: .875rem;
  color: var(--gray-700); cursor: pointer; transition: all .15s;
  text-align: center;
}
.grade-btn:hover { border-color: var(--orange-500); color: var(--orange-500); }
.grade-btn.selected {
  background: var(--orange-500); border-color: var(--orange-500);
  color: #fff; font-weight: 600;
}

/* Inputs */
textarea, input[type="text"] {
  width: 100%; border: 1.5px solid var(--gray-200); border-radius: 10px;
  padding: 12px 14px; font-size: .95rem; font-family: inherit;
  color: var(--gray-900); resize: vertical; transition: border-color .15s;
  background: #fff;
}
textarea:focus, input[type="text"]:focus {
  outline: none; border-color: var(--orange-500);
  box-shadow: 0 0 0 3px rgba(249,115,22,.12);
}

/* Optional label */
.optional-tag {
  font-size: .78rem; font-weight: 400; color: var(--gray-400);
  margin-left: 4px;
}

/* ── Validation error ── */
.field-error { font-size: .82rem; color: #ef4444; margin-top: 6px; display: none; }
.field-error.visible { display: block; }

/* ── Submit button ── */
.btn-primary {
  display: block; width: 100%; padding: 15px;
  background: var(--orange-500); color: #fff;
  border: none; border-radius: 12px; font-size: 1.05rem; font-weight: 700;
  cursor: pointer; transition: background .15s, transform .1s;
  letter-spacing: .04em;
}
.btn-primary:hover:not(:disabled) { background: var(--orange-600); transform: translateY(-1px); }
.btn-primary:disabled { opacity: .65; cursor: not-allowed; transform: none; }

/* ── Result page ── */
.result-header { margin-bottom: 24px; }
.result-header h2 { font-size: 1.4rem; font-weight: 800; color: var(--gray-900); margin-bottom: 6px; }
.result-header p { font-size: .9rem; color: var(--gray-500); }

/* Module card */
.module-card {
  background: #fff; border-radius: var(--radius); box-shadow: var(--shadow);
  padding: 22px 24px; margin-bottom: 16px;
  border-left: 4px solid var(--orange-500);
}
@media (max-width: 500px) { .module-card { padding: 16px; } }

.module-label {
  font-size: .78rem; font-weight: 700; color: var(--orange-500);
  text-transform: uppercase; letter-spacing: .08em; margin-bottom: 6px;
}
.module-title { font-size: 1rem; font-weight: 700; color: var(--gray-900); margin-bottom: 12px; }
.module-body { font-size: .95rem; color: var(--gray-700); line-height: 1.7; }

/* stuck_points list */
.stuck-list { list-style: none; padding: 0; }
.stuck-list li {
  padding: 8px 12px; background: var(--orange-50);
  border-radius: 8px; margin-bottom: 8px;
  font-size: .93rem; color: var(--gray-700); line-height: 1.6;
}

/* direct_script box */
.script-box {
  background: var(--orange-50); border-radius: 10px; padding: 16px;
  font-size: .95rem; color: var(--gray-900); line-height: 1.75;
  border-left: 3px solid var(--orange-500); white-space: pre-wrap;
}

/* three_steps */
.three-steps { display: flex; flex-direction: column; gap: 12px; }
.step-item { display: flex; gap: 12px; align-items: flex-start; }
.step-icon {
  flex-shrink: 0; width: 28px; height: 28px; border-radius: 50%;
  background: var(--orange-500); color: #fff; font-size: .78rem; font-weight: 700;
  display: flex; align-items: center; justify-content: center; margin-top: 1px;
}
.step-content { flex: 1; }
.step-label { font-size: .82rem; font-weight: 700; color: var(--orange-600); margin-bottom: 3px; }
.step-text { font-size: .93rem; color: var(--gray-700); line-height: 1.6; }

/* ── Action buttons ── */
.action-row { display: flex; gap: 12px; margin-top: 24px; }
.btn-secondary {
  flex: 1; padding: 13px; border: 1.5px solid var(--gray-200);
  border-radius: 12px; background: #fff; font-size: .95rem; font-weight: 600;
  color: var(--gray-700); cursor: pointer; transition: all .15s;
}
.btn-secondary:hover { border-color: var(--orange-500); color: var(--orange-500); }
.btn-copy {
  flex: 1; padding: 13px; border: 1.5px solid var(--orange-500);
  border-radius: 12px; background: #fff; font-size: .95rem; font-weight: 600;
  color: var(--orange-500); cursor: pointer; transition: all .15s;
}
.btn-copy:hover { background: var(--orange-50); }
@media (max-width: 420px) { .action-row { flex-direction: column; } }

/* ── Error banner ── */
.error-banner {
  background: #fef2f2; border: 1px solid #fecaca; border-radius: 10px;
  padding: 14px 16px; color: #b91c1c; font-size: .9rem;
  margin-bottom: 16px; display: none;
}
.error-banner.visible { display: block; }

/* ── View toggle ── */
.view { display: none; }
.view.active { display: block; }
```

- [ ] **Step 2: Verify file was created**

```bash
ls -la /Users/yangming/datizi/static/style2.css
```
Expected: file exists, non-zero size.

- [ ] **Step 3: Commit**

```bash
cd /Users/yangming/datizi
git add static/style2.css
git commit -m "feat: add style2.css for 搭梯子 2.0"
```

---

## Task 3: Create static/app2.js

**Files:**
- Create: `static/app2.js`

- [ ] **Step 1: Create app2.js**

```javascript
// 搭梯子 2.0 — app2.js

(function () {
  // ── State ──────────────────────────────────────────
  let selectedGrade = "";
  let lastResult = null;

  // ── DOM refs ───────────────────────────────────────
  const viewInput  = document.getElementById("view-input");
  const viewResult = document.getElementById("view-result");

  const gradeButtons  = document.querySelectorAll(".grade-btn");
  const questionInput = document.getElementById("question");
  const studentInput  = document.getElementById("student-answer");
  const parentInput   = document.getElementById("parent-note");
  const submitBtn     = document.getElementById("submit-btn");
  const gradeError    = document.getElementById("grade-error");
  const questionError = document.getElementById("question-error");
  const errorBanner   = document.getElementById("error-banner");

  const resultContainer = document.getElementById("result-container");
  const btnBack         = document.getElementById("btn-back");
  const btnCopy         = document.getElementById("btn-copy");

  // ── Grade selection ────────────────────────────────
  gradeButtons.forEach(btn => {
    btn.addEventListener("click", () => {
      gradeButtons.forEach(b => b.classList.remove("selected"));
      btn.classList.add("selected");
      selectedGrade = btn.dataset.grade;
      gradeError.classList.remove("visible");
    });
  });

  // ── Form submit ────────────────────────────────────
  submitBtn.addEventListener("click", async () => {
    // Validate
    let valid = true;
    if (!selectedGrade) {
      gradeError.classList.add("visible");
      valid = false;
    }
    if (!questionInput.value.trim()) {
      questionError.classList.add("visible");
      valid = false;
    } else {
      questionError.classList.remove("visible");
    }
    if (!valid) return;

    // Loading state
    submitBtn.disabled = true;
    submitBtn.textContent = "马不停蹄生成中…";
    errorBanner.classList.remove("visible");

    try {
      const resp = await fetch("/api/v2/generate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          grade: selectedGrade,
          question: questionInput.value.trim(),
          student_answer: studentInput.value.trim(),
          parent_note: parentInput.value.trim(),
        }),
      });

      const json = await resp.json();

      if (!json.success) {
        throw new Error(json.error || "生成失败，请稍后再试。");
      }

      lastResult = json.data;
      renderResult(json.data);
      showView("result");

    } catch (err) {
      errorBanner.textContent = err.message || "生成失败，请稍后再试。";
      errorBanner.classList.add("visible");
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = "生成讲题方案";
    }
  });

  // ── Render result ──────────────────────────────────
  function renderResult(data) {
    // Module 1: start_from
    document.getElementById("m-start-from").textContent = data.start_from || "";

    // Module 2: stuck_points
    const stuckList = document.getElementById("m-stuck-list");
    stuckList.innerHTML = "";
    const points = Array.isArray(data.stuck_points) ? data.stuck_points : [data.stuck_points];
    points.forEach(p => {
      const li = document.createElement("li");
      li.textContent = p;
      stuckList.appendChild(li);
    });

    // Module 3: fallback_step
    document.getElementById("m-fallback").textContent = data.fallback_step || "";

    // Module 4: direct_script
    document.getElementById("m-script").textContent = data.direct_script || "";

    // Module 5: three_steps
    const ts = data.three_steps || {};
    document.getElementById("m-say-it").textContent    = ts.say_it    || "";
    document.getElementById("m-write-it").textContent  = ts.write_it  || "";
    document.getElementById("m-draw-it").textContent   = ts.draw_it   || "";
  }

  // ── Back button ────────────────────────────────────
  btnBack.addEventListener("click", () => {
    // Clear form
    gradeButtons.forEach(b => b.classList.remove("selected"));
    selectedGrade = "";
    questionInput.value = "";
    studentInput.value = "";
    parentInput.value = "";
    gradeError.classList.remove("visible");
    questionError.classList.remove("visible");
    errorBanner.classList.remove("visible");
    showView("input");
  });

  // ── Copy result ────────────────────────────────────
  btnCopy.addEventListener("click", async () => {
    if (!lastResult) return;
    const d = lastResult;
    const ts = d.three_steps || {};
    const stuckText = Array.isArray(d.stuck_points)
      ? d.stuck_points.map(p => `• ${p}`).join("\n")
      : `• ${d.stuck_points}`;

    const text = [
      `【这题先讲什么】\n${d.start_from}`,
      `【孩子最容易卡在哪】\n${stuckText}`,
      `【如果孩子没懂，退回哪一步】\n${d.fallback_step}`,
      `【你现在可以直接这样说】\n${d.direct_script}`,
      `【讲明白三步】\n先说出来：${ts.say_it}\n再写出来：${ts.write_it}\n必要时画出来：${ts.draw_it}`,
    ].join("\n\n");

    try {
      await navigator.clipboard.writeText(text);
      const orig = btnCopy.textContent;
      btnCopy.textContent = "已复制 ✓";
      setTimeout(() => { btnCopy.textContent = orig; }, 2000);
    } catch {
      // Fallback for browsers that block clipboard
      const ta = document.createElement("textarea");
      ta.value = text;
      ta.style.position = "fixed";
      ta.style.opacity = "0";
      document.body.appendChild(ta);
      ta.select();
      document.execCommand("copy");
      document.body.removeChild(ta);
      btnCopy.textContent = "已复制 ✓";
      setTimeout(() => { btnCopy.textContent = "复制这份讲题方案"; }, 2000);
    }
  });

  // ── View toggle ────────────────────────────────────
  function showView(name) {
    viewInput.classList.toggle("active",  name === "input");
    viewResult.classList.toggle("active", name === "result");
    window.scrollTo({ top: 0, behavior: "smooth" });
  }

  // Init: show input view
  showView("input");
})();
```

- [ ] **Step 2: Verify file created**

```bash
ls -la /Users/yangming/datizi/static/app2.js
```
Expected: file exists, non-zero size.

- [ ] **Step 3: Commit**

```bash
cd /Users/yangming/datizi
git add static/app2.js
git commit -m "feat: add app2.js for 搭梯子 2.0 frontend logic"
```

---

## Task 4: Create templates/index2.html

**Files:**
- Create: `templates/index2.html`

- [ ] **Step 1: Create index2.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>搭梯子 — 帮小学生家长把数学错题讲明白</title>
  <link rel="stylesheet" href="/static/style2.css">
</head>
<body>

<header class="site-header">
  <div class="header-icon">🪜</div>
  <h1>搭梯子</h1>
  <p class="tagline">帮小学生家长把数学错题讲明白</p>
</header>

<!-- ── INPUT VIEW ── -->
<div id="view-input" class="view">
  <div class="container">
    <div class="card">
      <p class="intro-text">
        当孩子来问这道题怎么做时，家长能借助《搭梯子》，像金牌名师一样，一步一步讲给孩子听，帮助孩子真正搞明白。
      </p>

      <!-- Grade -->
      <div class="form-group">
        <label class="form-label">孩子年级 <span class="required">*</span></label>
        <div class="grade-grid">
          <button class="grade-btn" data-grade="一年级">一年级</button>
          <button class="grade-btn" data-grade="二年级">二年级</button>
          <button class="grade-btn" data-grade="三年级">三年级</button>
          <button class="grade-btn" data-grade="四年级">四年级</button>
          <button class="grade-btn" data-grade="五年级">五年级</button>
          <button class="grade-btn" data-grade="六年级">六年级</button>
        </div>
        <p id="grade-error" class="field-error">请选择孩子年级</p>
      </div>

      <!-- Question -->
      <div class="form-group">
        <label class="form-label" for="question">题目内容 <span class="required">*</span></label>
        <textarea
          id="question"
          rows="5"
          placeholder="请输入一道孩子不会的数学题，例如：甲单独做一项工程，6天完成；乙单独做一项工程，8天完成。两人合作，几天完成？"
        ></textarea>
        <p id="question-error" class="field-error">请先输入题目内容</p>
      </div>

      <!-- Student answer (optional) -->
      <div class="form-group">
        <label class="form-label" for="student-answer">
          孩子原答案 <span class="optional-tag">（可选）</span>
        </label>
        <textarea id="student-answer" rows="2" placeholder="例如：14天"></textarea>
      </div>

      <!-- Parent note (optional) -->
      <div class="form-group">
        <label class="form-label" for="parent-note">
          家长补充说明 <span class="optional-tag">（可选）</span>
        </label>
        <textarea
          id="parent-note"
          rows="3"
          placeholder="例如：孩子总是不知道第一步该怎么想 / 我讲几遍他还是不懂 / 他不明白为什么这样列式"
        ></textarea>
      </div>

      <!-- Error banner -->
      <div id="error-banner" class="error-banner"></div>

      <!-- Submit -->
      <button id="submit-btn" class="btn-primary">生成讲题方案</button>
    </div>
  </div>
</div>

<!-- ── RESULT VIEW ── -->
<div id="view-result" class="view">
  <div class="container">
    <div class="result-header">
      <h2>这道题，你现在可以这样讲</h2>
      <p>你现在不用临时硬讲，按这个顺序讲，更容易让孩子听懂。</p>
    </div>

    <div id="result-container">

      <!-- Module 1 -->
      <div class="module-card">
        <div class="module-label">模块一</div>
        <div class="module-title">这题先讲什么</div>
        <div class="module-body" id="m-start-from"></div>
      </div>

      <!-- Module 2 -->
      <div class="module-card">
        <div class="module-label">模块二</div>
        <div class="module-title">孩子最容易卡在哪</div>
        <ul class="stuck-list" id="m-stuck-list"></ul>
      </div>

      <!-- Module 3 -->
      <div class="module-card">
        <div class="module-label">模块三</div>
        <div class="module-title">如果孩子没懂，退回哪一步</div>
        <div class="module-body" id="m-fallback"></div>
      </div>

      <!-- Module 4 -->
      <div class="module-card">
        <div class="module-label">模块四</div>
        <div class="module-title">你现在可以直接这样说</div>
        <div class="script-box" id="m-script"></div>
      </div>

      <!-- Module 5 -->
      <div class="module-card">
        <div class="module-label">模块五</div>
        <div class="module-title">讲明白三步</div>
        <div class="three-steps">
          <div class="step-item">
            <div class="step-icon">说</div>
            <div class="step-content">
              <div class="step-label">先说出来</div>
              <div class="step-text" id="m-say-it"></div>
            </div>
          </div>
          <div class="step-item">
            <div class="step-icon">写</div>
            <div class="step-content">
              <div class="step-label">再写出来</div>
              <div class="step-text" id="m-write-it"></div>
            </div>
          </div>
          <div class="step-item">
            <div class="step-icon">画</div>
            <div class="step-content">
              <div class="step-label">必要时画出来</div>
              <div class="step-text" id="m-draw-it"></div>
            </div>
          </div>
        </div>
      </div>

    </div><!-- /result-container -->

    <!-- Action buttons -->
    <div class="action-row">
      <button id="btn-back" class="btn-secondary">再分析一道题</button>
      <button id="btn-copy" class="btn-copy">复制这份讲题方案</button>
    </div>

  </div>
</div><!-- /view-result -->

<script src="/static/app2.js"></script>
</body>
</html>
```

- [ ] **Step 2: Verify file created**

```bash
ls -la /Users/yangming/datizi/templates/index2.html
```
Expected: file exists, non-zero size.

- [ ] **Step 3: Start server and verify the page loads**

```bash
cd /Users/yangming/datizi && uvicorn main:app --reload --port 8001 &
sleep 2
curl -s http://localhost:8001/v2 | grep -c "搭梯子"
```
Expected: output is `1` or more (title found in HTML).

Kill the test server after:
```bash
kill $(lsof -t -i:8001) 2>/dev/null || true
```

- [ ] **Step 4: Commit**

```bash
cd /Users/yangming/datizi
git add templates/index2.html
git commit -m "feat: add index2.html — 搭梯子 2.0 single-page UI"
```

---

## Task 5: End-to-end smoke test (mock mode)

**Files:** none (testing only)

- [ ] **Step 1: Start server**

```bash
cd /Users/yangming/datizi
# Temporarily unset API key to test mock mode
DEEPSEEK_API_KEY="" uvicorn main:app --port 8001 &
sleep 2
```

- [ ] **Step 2: Test API returns mock data**

```bash
curl -s -X POST http://localhost:8001/api/v2/generate \
  -H "Content-Type: application/json" \
  -d '{"grade":"五年级","question":"甲单独做一项工程，6天完成；乙单独做一项工程，8天完成。两人合作几天完成？"}' \
  | python3 -m json.tool
```

Expected: JSON with `"success": true` and `data` object containing `start_from`, `stuck_points`, `fallback_step`, `direct_script`, `three_steps`.

- [ ] **Step 3: Test validation — missing question**

```bash
curl -s -X POST http://localhost:8001/api/v2/generate \
  -H "Content-Type: application/json" \
  -d '{"grade":"五年级","question":""}' \
  | python3 -m json.tool
```

Expected: FastAPI returns 422 validation error (question is required).

- [ ] **Step 4: Stop server**

```bash
kill $(lsof -t -i:8001) 2>/dev/null || true
```

- [ ] **Step 5: Final commit**

```bash
cd /Users/yangming/datizi
git add -p  # review any unstaged changes
git commit -m "chore: 搭梯子 2.0 smoke test verified"
```

---

## Self-Review Checklist

### Spec Coverage

| Spec Section | Covered by |
|---|---|
| 5 output modules | Tasks 1, 3, 4 |
| Grade selector (1-6) | Task 4 |
| Question textarea + validation | Task 4 |
| Student answer (optional) | Task 4 |
| Parent note (optional) | Task 4 |
| Loading state "马不停蹄生成中…" | Task 3 |
| Error state display | Task 3, 4 |
| Result page with 5 module cards | Task 4 |
| Copy to clipboard | Task 3 |
| "再分析一道题" back button | Task 3 |
| POST /api/generate (mapped to /api/v2/generate) | Task 1 |
| Mock mode when no API key | Task 1 |
| DeepSeek-compatible API | Task 1 |
| JSON response format | Task 1 |
| Fallback JSON extraction | Task 1 (uses existing `extract_json_object`) |
| Mobile-friendly styles | Task 2 |
| Orange/warm color scheme | Task 2 |

### Placeholder Scan
No TBDs, no "implement later", all code blocks are complete.

### Type Consistency
- `GenerateV2Request` fields: `grade`, `question`, `student_answer`, `parent_note` — match JS `fetch` body keys
- API response keys: `start_from`, `stuck_points`, `fallback_step`, `direct_script`, `three_steps.{say_it,write_it,draw_it}` — consistent across Task 1 (Python), Task 3 (JS render), Task 4 (HTML element IDs)
- Element IDs in HTML: `m-start-from`, `m-stuck-list`, `m-fallback`, `m-script`, `m-say-it`, `m-write-it`, `m-draw-it` — all referenced by exact same name in app2.js

---

## Local Run Instructions

```bash
cd /Users/yangming/datizi
# Install deps if needed
pip install -r requirements.txt

# Option A: With real API key (edit .env first)
# echo "DEEPSEEK_API_KEY=sk-..." > .env

# Option B: Mock mode (no API key needed)
uvicorn main:app --reload --port 8000

# Open in browser:
# http://localhost:8000/v2
```
